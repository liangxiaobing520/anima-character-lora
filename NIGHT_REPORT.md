# 夜间任务报告

生成时间: 2026-09-15 00:27:02

## 分辨率 A/B 实验 (furina)

| 组 | 文件 | 大小 | 用途 |
|---|---|---|---|
| 768 | 缺失 | - | (可能 OOM) |
| 640 | `exp_furina_640.safetensors` | 88M | 对比基线 |

训练参数: dim 32 / alpha 16 / 1000 步 / lr 1e-4 / AdamW8bit / bf16，仅分辨率不同

### 逐张对照图

(未生成 —— 产物不全或 ComfyUI 启动失败)

同 prompt、同 seed、同 LoRA 强度 0.85，只有分辨率不同。
看头发挑染、帽檐、领结这类小结构，哪组更稳。

## 队列

- 分辨率: **640px**（用户决定；素材 ≥768 占 90%，无需放大）
- 采集 min-side 仍为 640（保素材量，10% 轻微放大影响可忽略）
- 日志: `/tmp/train_batch.log`
- 单角色预计 ~50 分钟（768 比 640 慢约 1.44×）

## 明早看这里

1. `/home/xiaozeng/lora_train/genshin_dataset/verify_compare` 里的对照图 —— 768 相对 640 的提升幅度，决定后续要不要统一口径
2. `/tmp/train_batch.log` 末尾 —— 队列跑完了几个角色
3. 本文件顶部时间戳 —— 夜班链路走到哪一步了

